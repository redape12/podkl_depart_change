CREATE TABLE AlexeyReport.temp_multiple_changes AS
SELECT 
    rel.ENTITY_ID,
    COUNT(*) as change_count
FROM 
    sitemanager.b_crm_event_relations rel
INNER JOIN 
    AlexeyReport.temp_all_depart tad ON rel.EVENT_ID = tad.EVENT_ID
GROUP BY 
    rel.ENTITY_ID
HAVING 
    COUNT(*) > 1;
